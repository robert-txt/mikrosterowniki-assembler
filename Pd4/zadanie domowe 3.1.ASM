	LJMP	START
	ORG	100H
START:
	LCALL	LCD_CLR		;CZYSZCZENIE EKRANU
	LCALL	WAIT_KEY	;POBIERANIE KODU KLAWISZA
	JZ	STOP		;JESLI 0 TO ZAKOÑCZ
	MOV	R0,A		;ZAPISZ KOD KLAWISZA W R0
	MOV	R1,#0		;WARTOŒÆ POCZ¥TKOWA
	MOV	R2,#1		;WARTOŒÆ POCZ¥TKOWA

L1:
	MOV	A,R1		;ZAPISANIE W A STARSZEJ LICZBY
	ADD	A,R2		;DODANIE STARSZEJ I M£ODZEJ
	MOV	R3,A		;ZAPISANIE WYNIKU W R3
	MOV	A,R2		;ZAPISANIE M£ODSZEJ LICZBY
	MOV	R1,A		;JAKO NOWEJ STARSZEJ
	MOV	A,R3		;ZAPISANIE WYNIKU DODAWANIA
	MOV	R2,A		;JAKO NOWEJ M£ODZEJ LICZBY
	DJNZ	R0,L1		;ZAPÊTLENIE, ZMNIEJSZA 
				;I SKACZE JEŒLI NIE 0
	MOV	A,R2		;OSTATECZNY WYNIK DO A
	LCALL	WRITE_HEX	;WYŒWIETLENIE WYNIKU

STOP:
	SJMP	STOP
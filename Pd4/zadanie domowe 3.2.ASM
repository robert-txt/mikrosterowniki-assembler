	LJMP	START
	ORG	100H
START:
	LCALL	LCD_CLR		;CZYSZCZENIE EKRANU
	MOV	R0,#8		;ILOSC CYKLI PETLI
				;(TYLE CO BITÓW)
	MOV	R1,#128		;PRZYPISANIE WARTOŒCI
				;MNO¯NIKA NAJSTARSZEGO
				;BITU
	MOV	R2,#0		;WYZEROWANIE WARTOŒCI
				;SUMY

L1:
	LCALL	WAIT_KEY	;POBRNIE KODU KLAWISZA
				;ZAK£ADAMY WPROWADZANIE
				;0 LUB 1
	JZ	L2		;JEŒLI A RÓWNE 0 SKOCZ
	MOV	A,R2		;WARTOŒÆ SUMY DO A
	ADD	A,R1		;DODANIE WARTOŒCI DLA 
				;R0-1 BITU
	MOV	R2,A		;ZAPIS WYNIKU DO R2
	
L2:
	MOV	B,#2		;WARTOŒÆ DZIELNIKA
	MOV	A,R1		;WARTOŒÆ DZIELNEJ
	DIV	AB		;ZMNIEJSZENIE WARTOŒCI NA
	MOV	R1,A		;WARTOŒÆ DLA M£ODSZEGO BITU
				;NP 128/2=64 64/2=32 ITD
	DJNZ	R0,L1		;ZAPÊTLENIE
	MOV	A,R2		;WYNIK DO A
	LCALL	WRITE_HEX	;WYŒWIETLENIE WYNIKU

STOP:
	SJMP	STOP
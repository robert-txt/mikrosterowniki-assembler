	LJMP	START
	ORG	100H

START:
	LCALL LCD_CLR

	MOV R0, #CSDS
	MOV A, #00111111B
	MOVX @R0,A	
	CLR TF0
	MOV TMOD, #00000001B	;GATE 0, CZAS WEWNÊTRZNY, TRYB 1
	MOV TL0, #0
L3:
	CLR C
	MOV R1, #100
L0:
	MOV TH0, #256-180
	SETB TR0
L1:
	JNB P3.5, L2
	SETB C
L2:
	JNB TF0,L1
	CLR TF0
	CLR TR0
	DJNZ R1, L0
	JC L3
	CLR P1.7
	
STOP:
	SJMP	STOP



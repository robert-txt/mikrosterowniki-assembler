	LJMP	START
	ORG	100H

START:
	LCALL LCD_CLR

	MOV TMOD, #01010000B	;GATE 0, C/T 1, TRYB 1

	MOV TH1, #0
	MOV TL1, #0
	MOV R0, #CSDS
	MOV A, #00111111B
	MOVX @R0, A
	SETB TR1
LOOP:
	MOV A, #10
	LCALL DELAY_100MS
	MOV A, TL1
	LCALL WRITE_HEX
	
	
	SJMP	LOOP

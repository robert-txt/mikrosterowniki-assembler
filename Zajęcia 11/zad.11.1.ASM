
	LJMP	START
	ORG	100H
START:
	LCALL LCD_CLR
	
LOOP:	LCALL WAIT_KEY
	ADD A, #'0'
	LCALL WR_DATA
	
	SJMP LOOP

WR_DATA:
	MOV R0, #LCDWD
	MOV R1, #LCDRC
	PUSH ACC

L11:
	MOVX A, @R1
	JB ACC.7, L11
	POP ACC
	MOVX @R0, A
	RET

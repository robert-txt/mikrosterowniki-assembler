
	LJMP	START
	ORG	100H
START:
	LCALL LCD_CLR
	
LOOP:	
	
	
	MOV	DPTR,#TEXT
	MOVC	A,@A+DPTR
	LCALL WR_DATA
	
	MOV	A,#4
	LCALL	DELAY_100MS	
	MOV	A,#00011100B	
	INC	A
	SJMP LOOP

WR_DATA:
	MOV R0, #LCDWD
	MOV R1, #LCDRC
	PUSH ACC
	

L11:
	MOVX A, @R1
	JB ACC.7, L11
	POP ACC
	MOVX @R0, A
	RET


TEXT:

	DB 'SPRZEDAM OPLA ',0	